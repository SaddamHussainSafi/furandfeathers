package com.furandfeathers.enums;

public enum ListingStatus {
    DRAFT,              // Pet saved as draft
    PENDING_REVIEW,     // Submitted for admin review
    APPROVED,           // Approved and visible to users
    REJECTED,           // Rejected by admin
    ADOPTED             // Pet has been adopted
}